# VIA Social feed reset behavior

Changing Following, Recent or Discovery clears the currently rendered post list before another public source is loaded. This avoids stale source data appearing beneath a newly selected feed label. No DeSo write action is introduced.
