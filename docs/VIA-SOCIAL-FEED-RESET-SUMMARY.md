# VIA Social feed reset

Following, Recent and Discovery are distinct public-read contexts. VIA clears rendered posts when the active mode changes, before another source is loaded. This prevents stale results from appearing beneath the wrong feed label and introduces no DeSo write action.
