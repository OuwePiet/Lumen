# VIA feed-state fix implemented

The Social public-read UI now resets rendered posts when the active feed changes. The next feed must be explicitly loaded, keeping Following, Recent and Discovery results separated and correctly labelled.
