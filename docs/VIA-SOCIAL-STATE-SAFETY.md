# VIA Social state safety

Rendered feed results belong only to the currently loaded public-read mode. A mode switch removes prior results before a new request, preventing misleading cross-mode presentation. This remains entirely read-only.
